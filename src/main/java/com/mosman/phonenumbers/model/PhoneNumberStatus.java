package com.mosman.phonenumbers.model;

/// Enum representing status of a PhoneNumber:
/// ACTIVE: the PhoneNumber is active
/// IN_ACTIVE: the PhoneNumber is in active
public enum PhoneNumberStatus {
    ACTIVE, IN_ACTIVE
}
